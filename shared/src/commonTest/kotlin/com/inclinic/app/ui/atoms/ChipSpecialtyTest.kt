package com.inclinic.app.ui.atoms

/**
 * Triangulation skipped: pure Composable, no extractable logic.
 *
 * ChipSpecialty is a stateless render-only composable whose only parameters are
 * a [String] label, a [Boolean] selected flag, and an optional click callback.
 * There are no pure helper functions with domain logic to unit-test in commonTest.
 * Visual correctness is verified via Pencil design parity and compile-time check.
 */
